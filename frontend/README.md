# Frontend Quick Start

Run the frontend from the repository root:

```bash
cd frontend
npm install
npm run dev
```

Then open the local URL shown in the terminal (usually `http://localhost:5173` or `http://localhost:5174`).
